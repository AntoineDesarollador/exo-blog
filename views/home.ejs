<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="css/style.css" rel="stylesheet" type="text/css">
    <title>Document</title>
</head>

<main id="home">
    <section>
        <h2>Voici une selection de mes stories mes petits chatons</h2>

        <% for(const story of stories) { %>
            <article>
                <h3><%= story.title %></h3>
                <p><%= story.content %></p>
                <p>posté le : <%= story.date.toLocaleDateString() %> à <%= story.date.toLocaleTimeString() %></p>
            </article>
        <% } %>
    </section>
</main>